name: Keepalive

on:
  schedule:
    - cron: '0 14 * * 1,4'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -I https://finanzas-360-alpha.vercel.app
